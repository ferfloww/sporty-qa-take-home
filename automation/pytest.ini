[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*

addopts =
    -v
    --strict-markers
    --tb=short
    --color=yes

markers =
    e2e: browser-driven end-to-end tests
    api: API-level tests, no browser required
    critical: covers a critical business rule or user journey